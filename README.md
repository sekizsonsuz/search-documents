# search-documents

### Parse docx and pdf files in folder and create json files in same folder:

```java -jar search-documents.jar --folder "/path/folder" --refresh```

### Search a keyword in previously created json files:

```java -jar search-documents.jar --folder "/path/folder" --search "keyword"```
